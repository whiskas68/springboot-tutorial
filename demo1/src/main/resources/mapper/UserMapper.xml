<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.whiskas68.booter.mapper.UserMapper">
    <resultMap id="user" type="com.whiskas68.booter.entity.User">
        <id property="id" column="id" javaType="Int"/>
        <result property="username" column="username" javaType="String"/>
        <result property="password" column="password" javaType="String"/>
    </resultMap>

    <select id="selectAll" resultType="com.whiskas68.booter.entity.User">
        select * from tb_user
    </select>

    <select id="selectById" resultType="com.whiskas68.booter.entity.User">
        select * from tb_user where id=#{id}
    </select>

    <insert id="insert" parameterType="com.whiskas68.booter.entity.User">
        insert into tb_user (username,password) values (#{username},#{password})
    </insert>



</mapper>